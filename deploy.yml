# Install necessary packages and updates
Write-Host "Updating Windows packages..."
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process -Force

# Install IIS Web Server (example)
Write-Host "Installing IIS..."
Install-WindowsFeature -Name Web-Server

# Other custom configurations or applications can be added here
Write-Host "Initialization script completed."
